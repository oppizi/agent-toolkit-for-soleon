{
  "name": "soleon-observer",
  "description": "Read-only access to Soleon. Inspect agents and their configuration, read traces, failures and usage, review eval scores, and browse ideas and the wiki. Requests no write consent at all — installing this plugin is the least-privilege way to connect Claude Code to a Soleon system.",
  "version": "0.3.1",
  "author": {
    "name": "Oppizi — Soleon Headless Toolkit"
  },
  "keywords": [
    "soleon",
    "agent-infra",
    "read-only",
    "observability",
    "least-privilege"
  ],
  "license": "MIT",
  "userConfig": {
    "server_url": {
      "type": "string",
      "title": "Soleon MCP server URL",
      "description": "Which Soleon system to connect to. Defaults to the dev system, which works out of the box. Changing this ALONE will break sign-in: each system registers its own OAuth client, and the client ID cannot be set from this screen. To use another environment, add the server with both values instead — `claude mcp add --transport http <url> --client-id <id>` (see the plugin README).",
      "required": false,
      "default": "https://mcp-dev.oppizi.com/mcp"
    }
  }
}
